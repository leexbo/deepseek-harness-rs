[package]
name = "liuma-compaction"
description = "上下文压缩策略:压力阈值/保留尾预算、压缩范围选段(tool 配对平衡)、checkpoint 摘要指令"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[lib]
crate-type = ["rlib"]

[dependencies]
liuma-session.workspace = true
serde_json.workspace = true
